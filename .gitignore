# Database (contains user data and password hashes — never commit)
*.db

# Uploaded project documents (client contracts and the like — never commit)
uploads/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/

# Env / secrets
.env

# Worktrees (Claude Code native and manual)
.claude/worktrees/
.worktrees/

# Database backups made by the run wrapper, and browser-driven verification output
*.db.bak-*
.playwright-mcp/

# Local test logins (plaintext passwords — never commit)
TEST_CREDENTIALS.md

# Design handoff reference material (kept locally, not part of the app)
design_handoff_dani_smith_design_system/
Invoice template creation.zip
