# Database (contains user data and password hashes — never commit)
*.db

# Uploaded project documents (client contracts and the like — never commit)
uploads/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/

# Env / secrets
.env

# Worktrees (Claude Code native and manual)
.claude/worktrees/
.worktrees/

# Database backups made by the run wrapper, and browser-driven verification output
*.db.bak-*
.playwright-mcp/
